/**
 * KeyCode enum to use in for example if's
 * 
 */

export enum KeyCode {
    Backspace = 8,
    Left = 37,
    Up = 38,
    Right = 39,
    Down = 40,
    Escape = 27,
    Enter = 13,
    Shift = 16
}
